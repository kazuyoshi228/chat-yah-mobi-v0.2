/**
 * @deprecated This component is no longer used.
 * /admin/active-chats redirects to /admin/chats (AdminChatList) in App.tsx.
 * Kept as an empty stub to avoid breaking any cached imports.
 */
export default function AdminChats() {
  return null;
}
